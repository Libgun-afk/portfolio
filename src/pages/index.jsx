import { Header } from "@/components/Header";
import { Text } from "@/components/Text";

export const Home = () => {
  return (
    <div>
      <Header />
      <Text />
    </div>
  );
};

export default Home;
